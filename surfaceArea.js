function calcSurfaceArea(base, height) {
    console.log('testicle connected');
    console.log('shit came in', base, height);

    //process
    const answer = 0.5 * base * height;
    console.log(answer);

    //output
    return answer;
}